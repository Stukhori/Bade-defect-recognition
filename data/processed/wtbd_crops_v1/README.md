# WTBD crops v1

Deterministic Phase 3 metadata for one 224x224 RGB PNG per curated WTBD defect instance. Generated PNG pixels live in `images/` and are intentionally ignored by Git. Regenerate from the immutable WTBD raw release with `python scripts/build_wtbd_crops.py --config configs/crop_dataset.yaml`.
