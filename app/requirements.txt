# Streamlit Community Cloud reads this file before the repository-level uv.lock
# because the entry point is app/app.py. Install the local package and reproduce
# the validated CPU application environment.
--extra-index-url https://download.pytorch.org/whl/cpu
-e .

streamlit==1.62.0
streamlit-cropper==0.3.1
numpy==2.4.6
Pillow==12.3.0
matplotlib==3.11.1
PyYAML==6.0.3
joblib==1.5.3
scikit-image==0.26.0
scikit-learn==1.9.0
torch==2.13.0+cpu; sys_platform != "darwin"
torch==2.13.0; sys_platform == "darwin"
torchvision==0.28.0+cpu; sys_platform != "darwin"
torchvision==0.28.0; sys_platform == "darwin"
